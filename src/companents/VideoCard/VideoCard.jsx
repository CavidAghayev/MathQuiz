import styles from '../VideoCard/VideoCard.module.css'
export const VideoCard = ({name,questionCount,classNumber}) => {
  return (
    <div className={styles.videocard}>
        <div className={styles["videocard__video"]}>

        </div>
        
        </div>
  )
}